{
  "name": "dlvideo",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && source venv/bin/activate && python -m uvicorn server:app --host 0.0.0.0 --port 8000 --reload\" \"cd frontend && yarn start\"",
    "dev": "npm run start",
    "backend": "cd backend && source venv/bin/activate && python -m uvicorn server:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "cd frontend && yarn start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}